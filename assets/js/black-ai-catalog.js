(()=>{
  const VERSION='20260913-4';
  const state={client:null,items:[],settings:{},search:'',caseFilter:'all',modeFilter:'all',treatmentFilter:'all',activeFilter:'active',loaded:false};
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const money=v=>Number(v||0).toLocaleString('es-AR',{style:'currency',currency:'ARS',maximumFractionDigits:0});
  const label=v=>({monofocal:'Monofocal',bifocal:'Bifocal',occupational:'Ocupacional',multifocal:'Multifocal',stock:'Stock',range_extended:'R.E',laboratory:'Laboratorio',antireflective:'Antirreflejo',blue_filter:'Filtro Blue',super_blue:'Super Blue',photochromic:'Fotocromático',photochromic_blue:'Fotocromático Blue',black_blue_4k:'Black Blue 4K',none:'Sin tratamiento'}[v]||v||'—');
  const defaults={standard_discount_percent:30,black_protect_discount_percent:40,rounding_step:100};
  function resolveClient(){try{if(window.parent&&window.parent!==window&&window.parent.BlackPortal?.getSupabase)return window.parent.BlackPortal.getSupabase()}catch(_){ }return window.BlackPortal?.getSupabase?.()||null}
  function pricingDefaults(){return {...defaults,...(state.settings?.catalogPricing||{})}}
  function productPricing(x){const p=x?.metadata?.commercial_pricing||{},d=pricingDefaults();return{standard_discount_percent:Number(p.standard_discount_percent??d.standard_discount_percent),black_protect_discount_percent:Number(p.black_protect_discount_percent??d.black_protect_discount_percent),rounding_step:Number(p.rounding_step??d.rounding_step),list_price_override:p.list_price_override==null?null:Number(p.list_price_override)}}
  function rounded(v,step){const s=Math.max(1,Number(step)||1);return Math.round(Number(v||0)/s)*s}
  function priceView(x){const p=productPricing(x),base=Number(x?.base_price||0),raw=p.standard_discount_percent>=100?base:base/(1-p.standard_discount_percent/100),list=p.list_price_override&&p.list_price_override>0?p.list_price_override:rounded(raw,p.rounding_step),protect=rounded(list*(1-p.black_protect_discount_percent/100),p.rounding_step);return{...p,base,list,protect}}

  function ensureShell(){
    if(!document.querySelector('link[data-catalog-css]')){const link=document.createElement('link');link.rel='stylesheet';link.href=`assets/css/black-ai-catalog.css?v=${VERSION}`;link.dataset.catalogCss='1';document.head.appendChild(link)}
    const nav=document.querySelector('.ai-tabs');
    if(nav&&!nav.querySelector('[data-tab="catalogo"]')){const knowledge=nav.querySelector('[data-tab="conocimiento"]'),btn=document.createElement('button');btn.className='ai-tab';btn.dataset.tab='catalogo';btn.type='button';btn.textContent='Catálogo';knowledge?.after(btn);btn.addEventListener('click',()=>activate())}
    if(!document.getElementById('tab-catalogo')){const panel=document.createElement('section');panel.className='tab-panel';panel.id='tab-catalogo';panel.innerHTML='<div class="catalog-loading">Cargando catálogo…</div>';document.getElementById('tab-conocimiento')?.after(panel)}
  }
  function activate(){document.querySelectorAll('.ai-tab').forEach(t=>t.classList.toggle('active',t.dataset.tab==='catalogo'));document.querySelectorAll('.tab-panel').forEach(p=>p.classList.toggle('active',p.id==='tab-catalogo'));if(!state.loaded)load();else render()}

  async function load(){
    const panel=document.getElementById('tab-catalogo');if(!panel)return;panel.innerHTML='<div class="catalog-loading">Leyendo productos desde Supabase…</div>';
    state.client=resolveClient();if(!state.client){panel.innerHTML='<div class="catalog-error">Supabase no está disponible.</div>';return}
    const [{data:products,error},{data:settings}]=await Promise.all([
      state.client.from('black_ai_products').select('id,name,base_price,optical_case,supply_mode,material,treatment,design,is_active,imported_at,updated_at,sphere_min,sphere_max,cylinder_min,cylinder_max,cylinder_abs_max,addition_min,addition_max,metadata').order('name',{ascending:true}),
      state.client.from('black_ai_settings').select('config').eq('id','global').maybeSingle()
    ]);
    if(error){panel.innerHTML=`<div class="catalog-error">${esc(error.message)}</div>`;return}
    state.items=products||[];state.settings=settings?.config||{};state.loaded=true;render();
  }

  function filtered(){const q=state.search.trim().toLowerCase();return state.items.filter(x=>{if(state.activeFilter==='active'&&!x.is_active)return false;if(state.activeFilter==='inactive'&&x.is_active)return false;if(state.caseFilter!=='all'&&x.optical_case!==state.caseFilter)return false;if(state.modeFilter!=='all'&&x.supply_mode!==state.modeFilter)return false;if(state.treatmentFilter!=='all'&&x.treatment!==state.treatmentFilter)return false;if(q&&!`${x.name||''} ${x.material||''} ${x.treatment||''} ${x.design||''}`.toLowerCase().includes(q))return false;return true})}
  const unique=key=>[...new Set(state.items.map(x=>x[key]).filter(Boolean))].sort();
  const lastUpdate=()=>state.items.map(x=>x.imported_at||x.updated_at).filter(Boolean).sort().at(-1)||null;
  const technicalReady=x=>[x.sphere_min,x.sphere_max,x.cylinder_min,x.cylinder_max,x.cylinder_abs_max,x.addition_min,x.addition_max].some(v=>v!==null&&v!==undefined);

  function render(){
    const panel=document.getElementById('tab-catalogo');if(!panel)return;const rows=filtered(),active=state.items.filter(x=>x.is_active).length,inactive=state.items.length-active,technical=state.items.filter(technicalReady).length,last=lastUpdate(),pd=pricingDefaults();
    panel.innerHTML=`
      <div class="section-intro catalog-intro"><div><h2>Catálogo</h2><p>Fuente comercial de Black AI. El precio SINERGIA es el precio final habitual; Black OS calcula automáticamente el precio de lista y Black Protect.</p></div><button class="btn-primary" id="catalog-import" type="button">Actualizar catálogo</button></div>
      <div class="catalog-pricing-rule"><div><span>DESCUENTO HABITUAL</span><input id="global-discount" type="number" min="0" max="99" step="0.1" value="${esc(pd.standard_discount_percent)}"><b>%</b></div><div><span>BLACK PROTECT</span><input id="global-protect" type="number" min="0" max="99" step="0.1" value="${esc(pd.black_protect_discount_percent)}"><b>%</b></div><div><span>REDONDEO</span><input id="global-round" type="number" min="1" step="1" value="${esc(pd.rounding_step)}"><b>$</b></div><button class="btn-secondary" id="save-global-pricing" type="button">Guardar regla general</button><small>Ejemplo: SINERGIA $98.000 + 30% comercial → lista aprox. $140.000. Black Protect 40% → $84.000.</small></div>
      <div class="catalog-stats"><div><span>Productos</span><strong>${state.items.length}</strong></div><div><span>Activos</span><strong>${active}</strong></div><div><span>Inactivos</span><strong>${inactive}</strong></div><div><span>Con ficha técnica</span><strong>${technical}</strong></div><div><span>Última actualización</span><strong>${last?new Date(last).toLocaleDateString('es-AR'):'—'}</strong></div></div>
      <div class="catalog-toolbar"><input id="catalog-search" value="${esc(state.search)}" placeholder="Buscar producto, material, tratamiento o diseño…"><select id="catalog-case"><option value="all">Todos los casos</option>${unique('optical_case').map(v=>`<option value="${esc(v)}" ${state.caseFilter===v?'selected':''}>${esc(label(v))}</option>`).join('')}</select><select id="catalog-mode"><option value="all">Todas las modalidades</option>${unique('supply_mode').map(v=>`<option value="${esc(v)}" ${state.modeFilter===v?'selected':''}>${esc(label(v))}</option>`).join('')}</select><select id="catalog-treatment"><option value="all">Todos los tratamientos</option>${unique('treatment').map(v=>`<option value="${esc(v)}" ${state.treatmentFilter===v?'selected':''}>${esc(label(v))}</option>`).join('')}</select><select id="catalog-active"><option value="active" ${state.activeFilter==='active'?'selected':''}>Activos</option><option value="all" ${state.activeFilter==='all'?'selected':''}>Todos</option><option value="inactive" ${state.activeFilter==='inactive'?'selected':''}>Inactivos</option></select></div>
      <div class="catalog-result-line"><strong>${rows.length}</strong> productos visibles</div>
      <div class="catalog-table-wrap"><table class="catalog-table"><thead><tr><th>Producto</th><th>Precio SINERGIA</th><th>Lista</th><th>Black Protect</th><th>Caso</th><th>Modalidad</th><th>Material</th><th>Tratamiento</th><th>Diseño</th><th>Estado técnico</th></tr></thead><tbody>${rows.map(rowHtml).join('')}</tbody></table>${rows.length?'':'<div class="catalog-empty">No hay productos que coincidan con los filtros.</div>'}</div>`;
    bind();
  }

  function rowHtml(x){const tech=technicalReady(x),p=priceView(x);return `<tr data-product="${x.id}" ${x.is_active?'':'style="opacity:.58"'}><td><div class="catalog-product"><strong>${esc(x.name)}</strong><small>${x.is_active?'Activo':'Inactivo · Black AI no lo ofrece'}</small></div></td><td class="catalog-price">${money(p.base)}</td><td class="catalog-price-list">${money(p.list)}</td><td class="catalog-price-protect">${money(p.protect)}</td><td>${esc(label(x.optical_case))}</td><td>${esc(label(x.supply_mode))}</td><td>${esc(x.material||'—')}</td><td>${esc(label(x.treatment))}</td><td>${esc(x.design||'—')}</td><td><span class="catalog-tech ${tech?'ready':'pending'}">${tech?'Configurado':'Pendiente'}</span></td></tr>`}

  async function saveGlobalPricing(){const btn=document.getElementById('save-global-pricing'),standard=Number(document.getElementById('global-discount')?.value),protect=Number(document.getElementById('global-protect')?.value),rounding=Number(document.getElementById('global-round')?.value);if(!(standard>=0&&standard<100&&protect>=0&&protect<100&&rounding>=1)){alert('Revisá los porcentajes y el redondeo.');return}btn.disabled=true;btn.textContent='Guardando…';const config={...state.settings,catalogPricing:{standard_discount_percent:standard,black_protect_discount_percent:protect,rounding_step:rounding}};const {error}=await state.client.from('black_ai_settings').update({config,updated_at:new Date().toISOString()}).eq('id','global');if(error){alert(error.message);btn.disabled=false;btn.textContent='Guardar regla general';return}state.settings=config;render()}

  function bind(){const search=document.getElementById('catalog-search');search?.addEventListener('input',e=>{state.search=e.target.value;render()});document.getElementById('catalog-case')?.addEventListener('change',e=>{state.caseFilter=e.target.value;render()});document.getElementById('catalog-mode')?.addEventListener('change',e=>{state.modeFilter=e.target.value;render()});document.getElementById('catalog-treatment')?.addEventListener('change',e=>{state.treatmentFilter=e.target.value;render()});document.getElementById('catalog-active')?.addEventListener('change',e=>{state.activeFilter=e.target.value;render()});document.getElementById('save-global-pricing')?.addEventListener('click',saveGlobalPricing);document.getElementById('catalog-import')?.addEventListener('click',async()=>{if(!window.BlackAiCatalogImport){await new Promise((resolve,reject)=>{const s=document.createElement('script');s.src='assets/js/black-ai-catalog-import.js?v=20260913-4';s.onload=resolve;s.onerror=reject;document.body.appendChild(s)})}window.BlackAiCatalogImport?.open()});document.querySelectorAll('[data-product]').forEach(tr=>tr.addEventListener('click',()=>openDetail(state.items.find(x=>x.id===tr.dataset.product))))}

  async function setActive(x,value,button){if(!state.client||!x)return;const action=value?'reactivar':'inhabilitar';if(!confirm(`¿Querés ${action} este producto?\n\n${x.name}\n\n${value?'Volverá a estar disponible para Black AI.':'Black AI dejará de ofrecerlo y de usarlo para precios.'}`))return;if(button){button.disabled=true;button.textContent=value?'Reactivando…':'Inhabilitando…'}const {error}=await state.client.from('black_ai_products').update({is_active:value,updated_at:new Date().toISOString()}).eq('id',x.id);if(error){alert(error.message||'No se pudo actualizar el producto.');return}x.is_active=value;render()}

  async function saveProductPricing(x,wrap){const standardRaw=wrap.querySelector('[data-price-standard]').value,protectRaw=wrap.querySelector('[data-price-protect]').value,roundRaw=wrap.querySelector('[data-price-round]').value,listRaw=wrap.querySelector('[data-price-list]').value;const cp={};if(standardRaw!=='')cp.standard_discount_percent=Number(standardRaw);if(protectRaw!=='')cp.black_protect_discount_percent=Number(protectRaw);if(roundRaw!=='')cp.rounding_step=Number(roundRaw);if(listRaw!=='')cp.list_price_override=Number(listRaw);const metadata={...(x.metadata||{}),commercial_pricing:cp};const {error}=await state.client.from('black_ai_products').update({metadata,updated_at:new Date().toISOString()}).eq('id',x.id);if(error){alert(error.message);return false}x.metadata=metadata;return true}

  function openDetail(x){
    if(!x)return;const tech=technicalReady(x),p=priceView(x),local=x?.metadata?.commercial_pricing||{},wrap=document.createElement('div');wrap.className='catalog-detail-backdrop';wrap.innerHTML=`<div class="catalog-detail"><button class="catalog-detail-close">×</button><span class="card-kicker">PRODUCTO</span><h3>${esc(x.name)}</h3><div class="catalog-detail-grid"><div><span>Precio SINERGIA / final</span><strong>${money(p.base)}</strong></div><div><span>Precio de lista</span><strong>${money(p.list)}</strong></div><div><span>Black Protect</span><strong>${money(p.protect)}</strong></div><div><span>Estado</span><strong>${x.is_active?'Activo':'Inactivo'}</strong></div><div><span>Caso</span><strong>${esc(label(x.optical_case))}</strong></div><div><span>Diseño</span><strong>${esc(x.design||'—')}</strong></div></div>
    <div class="catalog-detail-section"><h4>Presentación comercial</h4><p>Dejá vacío cualquier campo para usar la regla general del catálogo.</p><div class="catalog-price-editor"><label><span>Descuento habitual %</span><input data-price-standard type="number" min="0" max="99" step="0.1" placeholder="${p.standard_discount_percent}" value="${local.standard_discount_percent??''}"></label><label><span>Black Protect %</span><input data-price-protect type="number" min="0" max="99" step="0.1" placeholder="${p.black_protect_discount_percent}" value="${local.black_protect_discount_percent??''}"></label><label><span>Redondeo $</span><input data-price-round type="number" min="1" step="1" placeholder="${p.rounding_step}" value="${local.rounding_step??''}"></label><label><span>Precio lista manual</span><input data-price-list type="number" min="0" step="1" placeholder="Automático" value="${local.list_price_override??''}"></label></div><div class="catalog-whatsapp-preview"><span>Vista WhatsApp</span><code>~${money(p.list)}~ 👉🏻 *${money(p.base)}*</code><small>Black Protect: ~${money(p.list)}~ 👉🏻 *${money(p.protect)}*</small></div><button class="btn-primary" type="button" data-save-pricing>Guardar configuración comercial</button></div>
    <div class="catalog-detail-section"><h4>Información técnica</h4>${tech?`<div class="catalog-range-grid"><span>Esfera: ${esc(x.sphere_min??'—')} a ${esc(x.sphere_max??'—')}</span><span>Cilindro: ${esc(x.cylinder_min??'—')} a ${esc(x.cylinder_max??x.cylinder_abs_max??'—')}</span><span>Adición: ${esc(x.addition_min??'—')} a ${esc(x.addition_max??'—')}</span></div>`:'<p>Este producto todavía no tiene rangos ópticos cargados.</p>'}</div><div class="catalog-detail-section"><h4>Disponibilidad para Black AI</h4><p>${x.is_active?'Este producto puede aparecer en consultas y propuestas.':'Este producto está inhabilitado y Black AI no debe ofrecerlo.'}</p><button class="${x.is_active?'btn-secondary':'btn-primary'}" type="button" data-toggle-product>${x.is_active?'Inhabilitar producto':'Reactivar producto'}</button></div></div>`;
    document.body.appendChild(wrap);const close=()=>wrap.remove();wrap.querySelector('.catalog-detail-close').onclick=close;wrap.onclick=e=>{if(e.target===wrap)close()};wrap.querySelector('[data-toggle-product]')?.addEventListener('click',async e=>{await setActive(x,!x.is_active,e.currentTarget);close()});wrap.querySelector('[data-save-pricing]')?.addEventListener('click',async e=>{const btn=e.currentTarget;btn.disabled=true;btn.textContent='Guardando…';if(await saveProductPricing(x,wrap)){close();render()}else{btn.disabled=false;btn.textContent='Guardar configuración comercial'}})
  }

  ensureShell();window.BlackAiCatalog={activate,reload:async()=>{state.loaded=false;await load()}};
})();